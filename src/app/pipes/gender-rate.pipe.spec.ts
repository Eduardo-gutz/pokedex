import { GenderRatePipe } from './gender-rate.pipe';

describe('GenderRatePipe', () => {
  it('create an instance', () => {
    const pipe = new GenderRatePipe();
    expect(pipe).toBeTruthy();
  });
});

import { CaptureRatePipe } from './capture-rate.pipe';

describe('CaptureRatePipe', () => {
  it('create an instance', () => {
    const pipe = new CaptureRatePipe();
    expect(pipe).toBeTruthy();
  });
});
